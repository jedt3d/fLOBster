name: flobster_desktop
description: macOS-first desktop client for the fLOBster line of business framework.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.5

resolution: workspace

dependencies:
  flobster_client:
    path: ../../server/flobster_client
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
